{
  "name": "aic-app-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server for AIC App — DB (SELECT-only), blueprints and project guidelines",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "dotenv": "^16.5.0",
    "mysql2": "^3.14.1",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
